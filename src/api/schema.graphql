enum Gender {
  Male
  Female
}

type User {
  id: ID!
  first_name: String!
  last_name: String!
  email: String!
  gender: Gender
  ip_address: String
}

type Query {
  users: [User!]!
  user(id: ID!): User!
}
